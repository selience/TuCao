<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/windowBackground</item>
        <item name="actionMenuTextColor">@color/color_black</item>
        <item name="actionMenuTextAppearance">@style/MenuTextStyle</item>
        <item name="android:dropDownListViewStyle">@style/DropListViewStyle</item>
    </style>

    <style name="MenuTextStyle">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="DropListViewStyle" parent="@android:style/Widget.ListView.DropDown">
        <item name="android:dividerHeight">0.5dp</item>
        <item name="android:divider">@color/color_item_divide</item>
    </style>
</resources>
